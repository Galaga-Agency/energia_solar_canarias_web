export interface Project {
  id:        string
  slug:      string
  service:   string   // the service this case study illustrates
  sector:    string   // sector · island
  title:     string
  challenge: string
  solution:  string
  result:    string
  metric:    string   // headline figure
  image:     string
}
